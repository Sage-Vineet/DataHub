packages:
  - "apps/*"
  - "packages/*"
  # The legacy backend participates in the workspace (single install) but is
  # not edited — it is proxied to by apps/api and retired domain-by-domain.
  - "backend"
