# Auto-load the DataHub dev shell (flake-wrapped devenv) via direnv.
# First time: run `direnv allow`. Start Postgres with `devenv up` (or the `db-up` script).
watch_file devenv.nix
watch_file devenv.yaml
use flake . --impure
